{% extends 'base.html.twig' %}

{% block title %}New Produit{% endblock %}

{% block body %}
    <h1>Create new Produit</h1>

    <div class="card">
        <div class="card-body">
            {{ include('produit/_form.html.twig') }}
        </div>
    </div>

    <a class="btn btn-primary" href="{{ path('app_produit_index') }}">back to list</a>
{% endblock %}
