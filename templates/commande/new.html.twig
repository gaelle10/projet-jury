{% extends 'base.html.twig' %}

{% block title %}New Commande{% endblock %}

{% block body %}
    <h1>Create new Commande</h1>

    <div class="card">
        <div class="card-body">
            {{ include('commande/_form.html.twig') }}
        </div>
    </div>

    <a class="btn btn-primary" href="{{ path('app_commande_index') }}">back to list</a>
{% endblock %}
